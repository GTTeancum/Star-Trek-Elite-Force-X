# RedRocket receiver — 2026-09-13

User authorized reading the handoff and continuing on this PC. Hostname is RedRocket, user LRPC. Memory reduction remains the priority gate; no station/UI work resumed.

The primary handoff predates additional TARDIS work found in `build/research/ps2_re/RECEIVER_PROGRESS.md`. The receiver R5900 project and geometry export completed and saved successfully (end of `geometry_analysis.log`, 43 functions). Do not repeat the interrupted original import. The packed-index streaming candidate was already applied in `code/qcommon/cm_load_xbox.cpp` and `code/renderer/tr_bsp_xbox.cpp`; `.before` files and compile-only objects exist in `index_stream_candidate`. It is not runtime-qualified.

## Machine setup and preserved evidence

- Installed Python 3.12.10 via the installed Python manager; created a fresh repository `.venv` and installed the exact handoff requirements. All seven imports pass.
- Java 21.0.12 exists at `C:\Program Files\Java\jdk-21.0.12`; preserved repository Temurin 21.0.10+7 also exists. Shared Ghidra 11.0.1 and isolated compiled R5900 extension are present.
- Clean XDK5558 compiler and VS8 MASM execute successfully. Existing Bink path is present.
- Downloaded official pinned XEMU 0.8.134 Windows x86_64 to `%LOCALAPPDATA%\STEFXTools\xemu-0.8.134`; archive digest matched GitHub release metadata. ZIP SHA256 `3d32bf3c1c73d5469d860e1e3e0b9a83bef38d11bbc5d18b9340b12160124a1f`; EXE SHA256 `f59a9df35f7d3be20da091d14066d4670e97ec9ee27ddf1d9d4b1abf99d2fe72`. Codex package path redirection may resolve LOCALAPPDATA under its LocalCache.
- QCOW3 HDD header has zero backing offset/length, so no backing-file chain is required; virtual disk size 8,589,934,592 bytes. Original saves/disk retained.
- Baseline MAP/EXE/XBE pairs for both personalities copied to `build/research/ps2_re/redrocket_20260913/baseline_symbols`. SP hash matches handoff `0c11186d...`; no baseline overwrite.
- Git requires a command-scoped safe.directory exception for this shared repository. No global Git configuration changed.

## Validation and source findings

`verify_index_stream_bytes.py` compares original whole-lump index bytes against per-surface ZIP reads and offset rebasing across 66 packaged maps: 517,602 surfaces and 9,262,728 compared bytes pass. This validates host input equivalence, not Xbox FS interleaving, rendered output or memory savings. Evidence: `redrocket_20260913/index_stream_equivalence.json`.

R5900 geometry export refines the comparison: `0x20d708` allocates a whole temporary lump, then `0x20d6b0` seeks and reads it from the world file handle. `0x20e278` loads surfaces, vertices and indices concurrently, builds permanent surfaces, then frees all three temporary buffers. It checks widths 104/44/4, allocates 16-byte surface descriptors, and dispatches by surface type. `0x20ded8` allocates triangle data as 64 + 44*numVerts + 4*numIndices bytes. Thus the Xbox candidate is an independently supported lifetime improvement from the comparison, not a claim that PS2 already streams per-surface indices. Decompiler warnings still require assembly cross-check before stronger claims.

Harness changes: `run_sp_perf_probe.py --preserve-existing-artifacts` skips automatic cleanup of transferred evidence. `ja_xemu_smoke.py` now scopes explicit symbol lookups and personality/texture readers to the supplied bundle rather than mutable release files. Python syntax checks pass. Initial baseline flight recording explicitly uses the immutable bundle; legacy auxiliary readers in that already-started process retain their old behavior, so evaluate those separately.

## Active work

Fresh `-Target spmp -SkipAssets -SkipStage -FrameDiagnostics` build passed for both personalities; log `redrocket_20260913/build_spmp.log`. No reused-object shortcut on this first receiver build. Both builds also pass after diagnostic memory instrumentation (`build_measurement.log`, incremental objects now validated on this host).

Baseline probe `rr_mem_baseline_deck09` and candidate `rr_mem_stream_deck09` ended live/error-clear/in-game, restored the pipeline ISO and closed owned XEMU. Native captures individually reviewed (02:06:38 and 02:12:46); same Deck09 view/HUD, animated weapon glow differs. Config: 64 MiB guest RAM, native scale, 480p/16:9, audible 0.65, no host input. These are memory/functional checks, not performance acceptance while a build runs. Baseline final zone sample used/free/largest: 12,856,395 / 9,628,754 / 6,767,584; initial streaming candidate: 12,858,049 / 9,618,847 / 6,766,080. No steady-state improvement established.

The initial preservation option guarded entry cleanup but missed the final cleanup invocation. Baseline ISO restoration and evidence copy succeeded first; final cleanup then ran through its generated-directory section and aborted on Git ownership before scripts/output enumeration. Corrected both invocations before later runs. All handoff artifact-manifest paths still exist and Git reports no deleted tracked files; exact deletion of unlisted ignored generated artifacts cannot be reconstructed because that cleanup did not log individual removals. Do not claim the entire ignored tree was unchanged. Baseline summary was generated separately from preserved final dumps after fixing the harness.

Diagnostic measurement bundle: `redrocket_20260913/measurement_symbols`, SP SHA256 `6c69836643c0e5a8fdd6b5e786c737c344be819bda7df1e76fbc66aabd8894ca`. Diagnostic-only `stefx_index_stream` (default1) chooses streaming or whole-file control. Production always streams. `_g_SPXBIndexStreamProof[64]` is magic0x49534d31, enabled, indexFileBytes, completedMask, then six ten-DWORD snapshots: physicalFree/zoneSize/zoneUsed/zoneFree/largest/zoneLifetimePeak/overhead/filesys/bsp/freeBlocks. Phases: before indices, indices ready, triangles done, faces done, indices released, flares done. The control releases its file immediately after faces (slightly earlier than the original pre-candidate scope end); comparisons during triangles/faces preserve whole-file residency. The recorder retains stable double-read snapshots; these are not continuous extrema or an atomic cross-thread publication.

`rr_mem_control_deck09` completed, restored and closed. Whole-file control index bytes545646 lowered physical free6578176→6029312 at phase1 (548864 bytes), with zone usage unchanged. Source confirms `FS_ReadFile` routes buffers>=256KiB through `STEFX_AllocHeapFileBuffer`; do not label that allocation as zone memory. Streaming comparison `rr_mem_measured_stream_deck09` is running; await terminal state before another probe. No measured saving claimed until the paired comparison.

## Completed paired memory result

The prior turn made concrete progress: fresh builds, native Deck09 checks, and completed same-binary memory comparison. On continuation the measured-stream process was absent, its transaction was restored, and its final report/summary proved live, in-game, error-clear completion. No process was restarted from a timeout assumption.

`redrocket_20260913/deck09_memory_comparison.json` compares `rr_mem_control_deck09` and `rr_mem_measured_stream_deck09`, both SP hash `6c698366...`, complete snapshot mask63 and identical pre-index state. During indices-ready, triangles-done and faces-done, streaming has **548,864 more physical bytes free**, **73,952 more zone bytes used** (65,760 filesystem state +8,192 scratch), and unchanged zone capacity. Zone free falls73,972 including20 bytes overhead. At faces-done the largest free block falls8,200 bytes and lifetime zone peak rises73,964. After release zone use matches again; physical free differs by -24,576 immediately and -16,384 after flares. These are bounded phase samples, not a demonstrated improvement of global minimum physical RAM or steady-state usage. This is a supported transient physical-footprint reduction with an explicitly measured zone tradeoff, not a pool-budget transfer.

Next memory qualification: populated maps, save/load, repeated transitions and residency. `rr_mem_save_deck02` is running with write/overwrite/two reload commands and the same measurement bundle. Await final state and restoration before launching another test. Full VV interactions remain behind the memory qualification gate.

No commit, push or publication.

## Receiver checkpoint — September13, 16:50

Both later probes are terminal, ISO-restored and XEMU-closed. `redrocket_20260913/memory_qualification.json` contains values extracted from exact run summaries, final dumps and flight streams.

- `rr_mem_save_deck02_unc`: two writes (including overwrite), two loads, zero signature failures; final live/error-clear on tour/deck02. Physical sample minimum1,744,896 bytes; zone free minimum10,218,576; largest minimum6,708,288. Final static textures8,029,056/10,485,760; skin peak63,360/2,097,152 with zero swaps/fetches. Native captures10:35:54 and10:36:07 individually reviewed.
- `rr_mem_transporter_bridge`: process-local approach plus existing travel-menu handler invoked the authored Deck04→Deck01 route. Final live/error-clear on populated bridge. Save proof(1,1,0,0) is a transition write, not an ordinary save roundtrip. Final physical free1,433,600 bytes; recorded minimum221,184 (216KiB), zone free minimum6,369,498 and largest4,496,005. Final static textures7,999,872 and skin peak223,616, zero swaps/fetches. Native captures10:40:26,10:40:37 and10:41:13 individually reviewed.
- The bridge physical low is during client state6 at t63.80–69.44, after the bridge index snapshots completed aroundt58.00. Last file hash3861170523 resolves to models/players/alexandria/head_default-8.skin (152bytes). That is only the last FS-read identity, NOT the proven memory offender. `bridge_pressure_file_hash.json` preserves this distinction.

The index change is supported and saves536KiB of physical RAM in matched Deck09 loading phases, but the later216KiB bridge low means broader pressure relief is unfinished. Keep memory priority before station implementation. Next useful work is to capture ownership/peaks through that media-loading phase, identify the allocations responsible, and test a supported lifetime or representation change. Do not merely replay stationary bridge checks or claim that initial transient saving closes the memory problem.

A proposed scratch merge also needs more evidence: saves use2,048,000 bytes of full-save input plus zlib workspace in TempAlloc, alongside512,000 bytes of compressed output in BonePool. Bink uses BOTH pools concurrently (AllocWrapper/BinkVideo::Allocate uses TempAlloc; decoded-frame storage uses BonePoolTempAlloc). Thus simple aliasing remains invalid; any smaller shared arena needs measured concurrent high-water bounds, not the assumption that movie and save phases never overlap.

The interrupted `rr_mem_save_deck02` run failed at native capture due unavailable Z: mapping. Its ISO restored and XEMU closed; excluded from qualification. Active copied config now uses UNC paths. Added firmware/EEPROM/symbol-sibling/capture-directory preflight before ISO mutation. The build script now derives the existing Bink library directory from the repository's Programming parent rather than hardcoding Z:. PowerShell ParseInput reports no syntax errors; Python syntax, three provenance tests and paused-start tests pass. No game-source change since the tested measurement bundle.

Current environment may advertise unavailable Z: as cwd; use explicit UNC workdir for commands. Do not rerun builds or probes solely because an old exec session handle is gone: inspect terminal manifests/reports first. No emulator or build is active at this checkpoint. Full goal remains active. No commit, push or publication.

## Fresh-observation instrumentation — follow-on work

Source inspection corrected the interpretation of the earlier bridge trace: g_SPXBPhysAvail is refreshed by Z_GetMemoryStats, not by every host recorder poll. The 101 repeated low values at t63.8–69.4 therefore do not establish a six-second low-memory duration or identify an active allocation from contemporaneous file/phase counters. The 216 KiB value remains an observed low; exact originating caller/time were not published in that build.

Added diagnostic-only g_SPXBMemoryObservations[64][20] and serial in z_memman_console.cpp. Each existing Z_GetMemoryStats call publishes guest GetTickCount, _ReturnAddress, physical free/total and zone size/used/overhead/peak/free/largest/free-block/tag counts while holding the existing zone mutex. Publication serial is written last. No extra query sites, allocation strategy changes or pool cuts. Static diagnostic cost5124 bytes before linker/page rounding. Reader rejects incomplete/racing rows, reports overwritten records, and never treats repeated polls as new observations. Six reader tests pass; Python syntax checks pass. Both-personality diagnostic build is running in build_observations.log; do not treat compile success as runtime qualification.

Analysis utility: build/research/ps2_re/scripts/analyze_memory_observations.py takes flight JSONL, paired MAP and output JSON; translates paired EXE/XBE sections for caller attribution and rejects missing fresh observations. Zone categories exclude externally allocated models. Source audit confirms MD3 still copies input into another allocation whereas MDR can adopt its input; cached models survive transitions until cache cleanup. These are candidates, not proven causes or qualified changes.

## Completed fresh-observation bridge probe

Both builds completed successfully (build_observations.log, exec1452 exit0). Preserved both EXE/MAP/XBE pairs in observation_symbols. SP SHA256873bd8000c15b53130b9cb41795c2b6d1b5bcea4046c964223ac49ecb3b4a1d4; MP05844b38041fca66dc7f6e0a7168956bd3d098a2db2435bbc02fa08cb522f6a3. Added optional final dumps of observation ring and serial. Six observation reader tests, three provenance tests and paused-start verifier pass; syntax checks pass. Provenance test requires repository .venv (bare Python lacks psutil).

rr_mem_bridge_observations completed (exec83355 exit0), restored pipeline ISO and closed owned XEMU. Same transporter replay/menu route,130-second diagnostic, audible0.65/native480p16:9. Final tour/deck01 live/error-clear/in-game. Native capture xemu-2026-09-13-22-02-26.png individually reviewed: populated bridge, geometry/consoles/HUD/weapon intact. Full evidence is build/research/letterbox_perf/rr_mem_bridge_observations_20260913_215939.*, summary and transaction beside it. Run wrapper run_bridge_observations.ps1 records exact invocation; never rerun same name over evidence.

bridge_observations.json is the final analysis; bridge_observations_partial.json is superseded.46 fresh publications, zero overwritten records. Minimum physical sample serial39, guest55293ms,221184 bytes. Caller runtime654307 (0x9fbe3), translated with paired EXE/XBE/MAP, is STEFX_LogMdrMemoryStats+19 in tr_model.obj. At that observation zone size22777824, used9795095, overhead266533, free12716196, largest6765312, model-tag bytes0, bsp655708, soundRaw53834, filesys1360646. This is a tight physical sample with ample zone capacity, NOT proof that a particular model owns all physical allocations. Current ring doesn't publish model name or preflight/load-start/load-done phase; all call the same logging helper. Existing full HDD log may contain the matching text, or a direct correlated asset context publication can disambiguate. Do not reuse the earlier last-FS-skin-name as allocation ownership.

Diagnostic ring adds5124 BSS bytes before section/page rounding. Current zone capacity is8192 bytes below prior measurement build; account for this instrumentation effect. No saving is claimed for this diagnostic build. Neither continuous physical extrema nor whole lifetime ownership is established.

More source audit: R_LoadMD3 copies its source to a second allocation on Xbox; MDR can adopt input. Potential optimization needs bounds/ownership/error-path and runtime qualification. Cached models are purged at RE_RegisterMedia_LevelLoadEnd, called in CL_FirstSnapshot; begin is before SV_ShutdownGameProgs, so blindly moving deletion to beginning may invalidate outgoing references. The generic low-memory branch sets r_modelpoolmegs0, BUT Xbox Sys_LowPhysicalMemory returns0, so do NOT claim the runtime cap is already0. Initial commentary making that claim was promptly corrected. Determine actual cvar before selecting lifetime policy. No memory policy changes made this follow-on turn.

Next useful work: identify the model/phase corresponding to fresh MDR low, measure external model residency/temporary overlap, implement and compare a supported lifetime/representation saving. Full memory gate and VV stations/routes/save/aspect/package scope remain active. No emulator/build remains active at this checkpoint. No commits/push/publication.

## Correlated model context follow-on

Previous turn was progress (fresh diagnostics and completed bridge test). This turn's call-graph audit rules out the tentative retained-prior-map model-cache explanation: SV_SpawnServer invokes SV_ClearLastLevel before new content construction, and SV_ClearLastLevel calls R_ModelFree, which deletes all cached model disk images and MDR frame bases. Generic RE_RegisterMedia_LevelLoadEnd timing alone omitted this Xbox teardown. Do not move cache deletion earlier or cut r_modelpoolmegs based on that superseded hypothesis.

Read-only scan of the closed3,047,882,752-byte QCOW container found no raw matching used=9795095 log text (exec50338 exit0). This is not filesystem extraction or proof that no logical log exists; it only failed to recover a match by raw scan.

Added diagnostic schema2 asset_context (last DWORD, unchanged5124-byte static footprint), FNV1a of phase+'|'+model name. STEFX_LogMdrMemoryStats calls Z_GetMemoryStatsForAsset, passing this context directly; shared helper publishes it under the zone mutex. Ordinary Z_GetMemoryStats records context0. Caller is passed from the public wrapper to preserve originating call-site identity. No shared last-asset context or ephemeral pointer. Reader supports schema1/2; seven tests pass, including context and unknown-schema handling. Analyzer --assets enumerates PK3 MDR names and retains all matching phase/model candidates, rather than silently choosing a hash collision.

Both incremental builds passed (build_asset_context.log, exec67261 exit0); z_memman_console.obj timestamp followed final schema2 source write. Immutable asset_context_symbols has SP1bb3914ebcb9440f88549c2ceb0cf9eabe007a3d944364567559642082722bda, MPbb7750e5e4e826f7d7369cb1472c685c8737d7227df09ac82043128e15ee8bcc. Probe rr_mem_bridge_asset_context running via exec53253 with same130sec route, audible/native config. run_bridge_observations.ps1 now accepts Name and BundleName and refuses existing transaction names. Await terminal report/restoration; do not launch duplicate.

## Asset identified and lossless representation candidate

rr_mem_bridge_asset_context is terminal: exec53253 exit0, ISO restored, XEMU closed. Final tour/deck01 live/error-clear/in-game; native22-13-14 capture individually viewed intact bridge/HUD/crew. Final analysis bridge_asset_context.json supersedes partial:47 fresh records, zero lost. Low serial39 guest57930ms,221184 physical free, same zone used9795095/free12716196, asset context3204281599 uniquely matches load-start|models/players/crewthin/lower.mdr among enumerated package MDR phase/name pairs. Serial40 load-done remains221184; crewthin/upper preflight41 remains221184, then its load-start42 uses additional zone (11836742 used) with physical unchanged. Hazardfemale lower/upper load-start33/36 are4567040/2076672 physical free; crewthin lower preflight38 is843776, then load-start39 drops622592 bytes. This identifies the sampled call, not total allocation ownership.

Package audit audit_mdr_animation_sharing.py validates71 compressed player MDR layouts. mdr_animation_sharing.json and newer mdr_animation_sharing_blocks.json: no identical whole animation regions or duplicate16-frame blocks for crewthin/hazardfemale. Other duplicate groups are stasis/stasis2, some already-shared Borg variants, warbot/boss. Do not implement crew sharing from this audit; it disproves that candidate.

A more useful host-only experiment compiled actual mdr_block_codec.h through audit_mdr_delta.cpp using XDK vc71 compiler and VS8 host headers/libs; executable/object under redrocket directory. Exact package source files for crewthin/hazardfemale lower/upper copied to mdr_delta_inputs (manifest mdr_delta_inputs.json with source lengths/hashes). No source assets modified.

mdr_delta_audit.tsv is the earlier3-mode result; mdr_transpose_audit.tsv is final6-mode result. Columns: path, raw-LZ bytes, byte-XOR-LZ, byte-subtract-LZ, transposed-LZ, byte-subtract+transpose-LZ,16-bit-subtract+transpose-LZ, per-block best bytes, six winner counts. These totals include1 type byte per block, exclude the unchanged offset table/geometry. All six variants encoded/decoded each block with the real existing codec; byte-for-byte comparison to source passed. Best per-model bytes (current→candidate): crewthin/lower581996→519220 (-62776), crewthin/upper1972027→1739604 (-232423), hazardfemale/lower819641→730737 (-88904), hazardfemale/upper2442614→2181289 (-261325). Total645428 bytes (~630KiB) smaller across these four animation streams. Not measured Xbox RAM savings or performance; current code has NO transform implementation yet.

Potential implementation: retain16-frame blocks and existing offset table, add self-describing block types for reversible transforms, select smallest raw/current-LZ/transformed-LZ, inverse transform after decode. Existing raw scratch16384 bytes and packed scratch32800 bytes can potentially supply transform workspace plus compressed candidate workspace without adding a new static pool: partition packed scratch into16384 work and16416 encoded bytes; Encode returning0 for a larger-than-workspace candidate is safe to reject since it cannot beat raw. The two-pass loader must count and write the same representations, preserve frame-cache lifetime invariants and fallback paths, validate each new type/length, and decode old raw/type1 blocks. Consider a diagnostic-only switch for same-binary current-vs-transform comparisons. Test actual helper roundtrips/malformed input/all applicable MDRs, then matched physical/zone/loading/frame-time/save/travel checks. These are design notes, not implemented or qualified behavior.

No build/emulator/analysis process remains active. Full memory gate and VV scope remain unfinished; no commit/push/publication.

## Frame codec implementation and active matched comparison — September14

Implemented code/renderer/mdr_frame_codec.h and integrated two-pass stream loader/frame decoder in tr_model.cpp. Types0raw/1original-LZ retained;2XOR,3byte delta,4byte transpose,5delta+transpose,6word-delta+transpose are lossless. Encoder chooses smallest including raw/old format; size pass and write pass use identical selection, validate return sizes, retain existing offset table and cache keys. Existing packed scratch32800 split into16384 work+16416 encoded bytes; no new static arena. Decoder validates type/layout/length, inverse transforms, and preserves frame-cache behavior. Diagnostic stefx_mdr_frame_codec0 selects legacy representations,1(default) candidate; production defaults candidate. No runtime qualification claimed yet.

Production helper test scripts/tests/mdr_frame_codec.cpp passes boundary strides2..1024,1..16frames, random/zero inputs, every type, truncated/invalid blocks, exact-size/undersized output, and byte equality. All71 player MDR files enumerated/test invoked;63 within existing1024-byte frame limit pass,8 existing-fallback layouts skipped (forge_boss lower/upper,forge_bossw lower/upper,scoutbot lower,stasis lower,stasis2 lower,vermin lower). Log mdr_frame_codec_test.log; exact inputs/manifest mdr_frame_test_inputs(.json). Four target sizes equal prior audit645428 total saved animation bytes. Assets staged in mdr_guest_reference using original model paths for RAM verification. Both builds pass (build_frame_codec.log, exec4530 exit0). Immutable frame_codec_symbols: SPdbfe2e285335f3e32c82623d37440c8fe06b7178a22bdcf61e392ffc3524ea9b; MP175dedc5f3f40fba217b0dc594293555c763c408722a37e7501cc64a7395dcbc.

Control rr_mdr_codec_control (codec0,160sec) terminal exec31818 exit0, ISO restored and XEMU closed; final tour/deck01/live/error-clear/in-game, native06-16-59 individually reviewed intact populated bridge/HUD. Raw prefix rr_mdr_codec_control_20260914_061411. Candidate rr_mdr_codec_candidate (codec1,same bundle160sec) now running exec91636. Await terminal state/restoration rather than relaunch. run_bridge_observations.ps1 accepts FrameCodec,Seconds,KeepGuestRam. Candidate retains final paused64MiB RAM via new optional --keep-guest-ram in run_sp_perf_probe.py/ja_xemu_smoke.py; full RAM was already captured for profile extraction, option preserves complete dump instead of deleting scratch. No extra runtime RAM allocation. Python syntax/provenance3tests/paused-start checks pass.

verify_guest_mdr_blocks.py now independently inverses all7 resident types before source-byte comparison. After candidate completes, use exact *_final_guest_ram.bin and *_final_registers.txt with --assets redrocket_20260913/mdr_guest_reference, preserve JSON output. Then analyze fresh memory observations for both with frame_codec_symbols and --assets build/release/BaseEF; compare same asset/phase occurrence and final/min physical/zone stats, capacities, liveness/errors/route, native captures and relevant frame-time limits. Save/reload and repeated transition qualification remain necessary after the initial comparison. Do not promote artifacts or close full goal from this narrow check.

## User priority change — September16

Latest explicit user instruction pivots current work to hunting FPS improvements learned from the PS2 implementation, targeting co-op and Holomatch. This explicitly reopens that optimization scope and supersedes the earlier restriction against reopening accepted co-op/MP work. Preserve existing improvements, audio, controls and fidelity; keep distinguishing demonstrated PS2 techniques from independent Xbox ideas. Full VV goal remains unfinished and is deferred behind this new priority. No FPS gain has yet been demonstrated from the PS2 RE.

User asked to choose a less credit-intensive model BEFORE this new investigation starts, considering Astra Low or5.6 Sol Medium. No new FPS investigation/build/test was started during this model-choice response. Current recommended default: Sol Medium for targeted profiling, source comparisons, implementation and harness checks; escalate selected ambiguous R5900/disassembly or architectural questions if needed. Model has not been changed by the assistant.

Latest status inspection confirmed both rr_mdr_codec_control/candidate terminal, restored, live/error-clear bridge. Candidate minimum fresh physical free634880 vs control221184; final1855488 vs1433600; final zone free10816594 vs10584231; both zone capacity22777824. Candidate64MiB RAM dump exists. Direct guest animation-byte validation and save/repeated-transition qualification remain incomplete; preserve this pending memory work while pivoting.
